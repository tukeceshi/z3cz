按用户要求最小化修复：URL 探测范围限定两级 body → body.data。

改动（packages/runtime/src/ai-interface/parse-poll-response.ts，parseOfficialVolcanoPollBody 第 98-106 行）：在现有探测（body.content.video_url、body.data.content.video_url）基础上，各层同时尝试裸 `video_url` 字段，即依次探测：
1. body.content.video_url（现有）
2. body.content.video_url（现有）→ body.data.video_url（新增）
   即两级中每级先 `content.video_url` 后 `video_url`：body.content.video_url、body.video_url、body.data.content.video_url、body.data.video_url。

`parsePollResponse`（mapping 路径）与其他层级（body.data.data 等）不动。

测试（parse-poll-response.test.ts）：新增用例覆盖裸 `video_url` 字段（顶层及 data 层），保证原有用例不回归；运行 runtime 包相关测试验证。