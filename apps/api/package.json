{
  "name": "@dafthunk/api",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --live-reload --port 3001",
    "test": "vitest run",
    "lint": "pnpm run format:check && eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "db:reset": "wrangler d1 execute DB --local --command=\"DROP TABLE IF EXISTS d1_migrations; DROP TABLE IF EXISTS api_tokens; DROP TABLE IF EXISTS executions; DROP TABLE IF EXISTS deployments; DROP TABLE IF EXISTS workflows; DROP TABLE IF EXISTS memberships; DROP TABLE IF EXISTS organizations; DROP TABLE IF EXISTS users;\"",
    "db:migrate": "wrangler d1 migrations apply DB --local",
    "db:generate": "drizzle-kit generate",
    "db:prod:migrate": "wrangler d1 migrations apply DB --remote --env production",
    "db:prod:reset": "wrangler d1 execute DB --remote --env production --command=\"DROP TABLE IF EXISTS d1_migrations; DROP TABLE IF EXISTS api_tokens; DROP TABLE IF EXISTS executions; DROP TABLE IF EXISTS deployments; DROP TABLE IF EXISTS workflows; DROP TABLE IF EXISTS memberships; DROP TABLE IF EXISTS organizations; DROP TABLE IF EXISTS users;\" && wrangler d1 migrations apply DB --remote --env production"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250504.0",
    "@eslint/js": "^9.26.0",
    "@types/node": "^22.15.3",
    "drizzle-kit": "^0.31.1",
    "eslint": "^9.26.0",
    "globals": "^15.15.0",
    "prettier": "^3.5.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.31.1",
    "vitest": "^3.1.2",
    "wrangler": "^4.14.1"
  },
  "dependencies": {
    "@dafthunk/types": "workspace:*",
    "@hono/oauth-providers": "^0.7.1",
    "@hono/zod-validator": "^0.5.0",
    "@sendgrid/mail": "^8.1.5",
    "cloudflare": "^4.2.0",
    "drizzle-orm": "^0.43.1",
    "hono": "^4.7.8",
    "jose": "^6.0.10",
    "jsonpath-plus": "^10.3.0",
    "nanoid": "^5.1.5",
    "twilio": "^5.5.2",
    "uuid": "^11.1.0",
    "zod": "^3.24.3"
  }
}
